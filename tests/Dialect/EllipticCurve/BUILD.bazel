load("//bazel:lit.bzl", "glob_lit_tests")

package(
    licenses = ["notice"],
)

glob_lit_tests(
    name = "all_tests",
    data = ["//tests:test_utilities"],
    driver = "//tests:run_lit.sh",
    # TODO(quanxi1): Temporarily comment out this test as the inverse op
    # now expects a montgomery form inputs. The test will be revised in the
    # last commit.
    exclude = ["elliptic_curve_to_field_runner.mlir"],
    test_file_exts = ["mlir"],
)
